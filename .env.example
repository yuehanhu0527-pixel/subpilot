# SubPilot environment variables — copy this file to .env and fill in values.
# Phase 0 needs none of these; the server starts without any configuration.

# Which LLM provider to use:
#   anthropic — the real agent (default, requires ANTHROPIC_API_KEY from Phase 3 on)
#   fake      — scripted demo mode (Phase 7; used automatically when no API key is set)
SUBPILOT_LLM_PROVIDER=anthropic

# Anthropic API key (needed from Phase 3 onward)
ANTHROPIC_API_KEY=

# Local data directory (Chroma store, embedding model cache) — default: ./data
# SUBPILOT_DATA_DIR=./data
